import math

lowBound = int(input("What's the lower bound?"))
upbound = int(input("What's the upper bound?"))
rang = upbound - lowBound
index = list(range(lowBound, upbound + 1))
print(index)

for i in range(rang):
    math.sqrt()